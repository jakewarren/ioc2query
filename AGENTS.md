# Linting
- Lint the codebase with `golangci-lint`

# Testing
- Run tests with `go test -v ./...`
- Use table-driven testing
- **Mandatory TDD**: Always write comprehensive, failing tests before implementing any feature.
- Ensure tests fail for the correct reasons; never "code to green" or use stubs merely to satisfy a test requirement.

